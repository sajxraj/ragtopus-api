export enum Role {
  SYSTEM = 'system',
  USER = 'user',
  ASSISTANT = 'assistant',
}

const Role = {}

const User = {
  SYSTEM: Role.SYSTEM,
}
console.log({ User, Role })

export enum ABC {
  SYSTEM = 'system',
  USER = 'user',
  ASSISTANT = 'assistant',
}

const ABC = {}

console.log(ABC, User)

export enum MEME {
  SYSTEM = 'system',
  USER = 'user',
  ASSISTANT = 'assistant',
}

const MEME = {}

export enum SMS {
  SYSTEM = 'system',
  USER = 'user',
  ASSISTANT = 'assistant',
}

const SMS = {}
